<resources>

    <style name="MenuDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="MenuAnimation">
        <item name="android:windowEnterAnimation">@anim/bottom_to_top_show</item>
        <item name="android:windowExitAnimation">@anim/top_to_bottom_hide</item>
    </style>
</resources>
